<div class="row">
    <div class="col-md-12">
        <div class="panel panel-default">
            <div class="panel-heading">
                <h3 class="panel-title">Users</h3>
            </div>
            <div class="panel-body">
                <a href="<?php echo $this->url('events', array('action'=>'add'));?>">Add Event</a>

                <?php if (isset($events)) : ?>
                <table class="table">
                    <thead>
                    <tr>
                        <th>Id</th>
                        <th>Full name</th>
                        <th></th>
                    </tr>
                    </thead>
                <?php foreach($events as $event): ?>
                    <tbody>
                    <tr>
                        <td><?php echo $event->getId(); ?></td>
                        <td><?php echo $event->getFullName(); ?></td>
                        <td>
                            <a href="<?php echo $this->url('event', array('action'=>'edit', 'id' => $event->getId()));?>">Edit</a> |
                            <a href="<?php echo $this->url('event', array('action'=>'delete', 'id' => $event->getId()));?>">Delete</a>
                        </td>
                    </tr>
                    </tbody>
                <?php endforeach; ?>
                </table>
                <?php endif; ?>
            </div>
        </div>
    </div>
</div>